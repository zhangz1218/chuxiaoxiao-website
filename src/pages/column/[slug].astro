---
import Layout from '../../layouts/layout.astro';
import { columns } from '../../data/columns';

export function getStaticPaths() {
  return columns.map((column) => ({
    params: { slug: column.slug },
  }));
}

const { slug } = Astro.params;
const column = columns.find(c => c.slug === slug);

if (!column) {
  return Astro.redirect('/column');
}

const categoryEmoji: Record<string, string> = {
  '观点': '💡',
  '诗歌': '🎭',
  '随笔': '📝'
};

// 简单的 Markdown 渲染
function renderMarkdown(content: string): string {
  const lines = content.split('\n');
  let html = '';
  let inList = false;

  for (let i = 0; i < lines.length; i++) {
    const line = lines[i];

    // 空行
    if (line.trim() === '') {
      if (inList) {
        html += '</ul>\n';
        inList = false;
      }
      continue;
    }

    // 分隔线
    if (line.trim() === '---') {
      if (inList) {
        html += '</ul>\n';
        inList = false;
      }
      html += '<hr class="my-8 border-gray-200 dark:border-gray-700" />\n';
      continue;
    }

    // H2
    if (line.startsWith('## ')) {
      if (inList) {
        html += '</ul>\n';
        inList = false;
      }
      html += `<h2 class="text-2xl font-bold text-gray-900 dark:text-white mt-10 mb-4">${line.slice(3)}</h2>\n`;
      continue;
    }

    // H3
    if (line.startsWith('### ')) {
      if (inList) {
        html += '</ul>\n';
        inList = false;
      }
      html += `<h3 class="text-xl font-semibold text-gray-900 dark:text-white mt-8 mb-3">${line.slice(4)}</h3>\n`;
      continue;
    }

    // 列表项
    if (line.startsWith('- ')) {
      if (!inList) {
        html += '<ul class="list-disc list-inside text-gray-700 dark:text-gray-300 my-4 space-y-2">\n';
        inList = true;
      }
      html += `<li>${line.slice(2)}</li>\n`;
      continue;
    }

    // 普通段落
    if (inList) {
      html += '</ul>\n';
      inList = false;
    }
    html += `<p class="text-gray-700 dark:text-gray-300 mb-4 leading-relaxed">${line}</p>\n`;
  }

  if (inList) {
    html += '</ul>\n';
  }

  return html;
}
---

<Layout title={`${column.title} | 楚萧萧专栏`}>
  <main class="max-w-3xl mx-auto px-6 py-12">
    <article>
      <header class="mb-8">
        <div class="flex items-center gap-3 mb-4">
          <span class="text-2xl">{categoryEmoji[column.category]}</span>
          <span class="text-sm text-gray-500 dark:text-gray-400">
            {column.category}
          </span>
          <span class="text-sm text-gray-400 dark:text-gray-500">·</span>
          <time class="text-sm text-gray-500 dark:text-gray-400">
            {column.date}
          </time>
        </div>
        <h1 class="text-3xl font-bold text-gray-900 dark:text-white mb-4">
          {column.title}
        </h1>
        {column.tags.length > 0 && (
          <div class="flex gap-2">
            {column.tags.map((tag) => (
              <span class="text-sm px-3 py-1 bg-gray-100 dark:bg-gray-800 text-gray-600 dark:text-gray-400 rounded-full">
                #{tag}
              </span>
            ))}
          </div>
        )}
      </header>

      <div class="prose prose-lg dark:prose-invert max-w-none" set:html={renderMarkdown(column.content)} />
    </article>

    <footer class="mt-12 pt-8 border-t border-gray-200 dark:border-gray-700">
      <a href="/column" class="text-red-500 hover:text-red-600 transition-colors">
        ← 返回专栏
      </a>
    </footer>
  </main>
</Layout>
