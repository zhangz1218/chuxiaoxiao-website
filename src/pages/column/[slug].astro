---
import Layout from '../../layouts/layout.astro';
import { columns } from '../../data/columns';

export function getStaticPaths() {
  return columns.map((column) => ({
    params: { slug: column.slug },
  }));
}

const { slug } = Astro.params;
const column = columns.find(c => c.slug === slug);

if (!column) {
  return Astro.redirect('/column');
}

const categoryEmoji: Record<string, string> = {
  '观点': '💡',
  '诗歌': '🎭',
  '随笔': '📝'
};
---

<Layout title={`${column.title} | 楚萧萧专栏`}>
  <main class="max-w-3xl mx-auto px-6 py-12">
    <article>
      <header class="mb-8">
        <div class="flex items-center gap-3 mb-4">
          <span class="text-2xl">{categoryEmoji[column.category]}</span>
          <span class="text-sm text-gray-500 dark:text-gray-400">
            {column.category}
          </span>
          <span class="text-sm text-gray-400 dark:text-gray-500">·</span>
          <time class="text-sm text-gray-500 dark:text-gray-400">
            {column.date}
          </time>
        </div>
        <h1 class="text-3xl font-bold text-gray-900 dark:text-white mb-4">
          {column.title}
        </h1>
        {column.tags.length > 0 && (
          <div class="flex gap-2">
            {column.tags.map((tag) => (
              <span class="text-sm px-3 py-1 bg-gray-100 dark:bg-gray-800 text-gray-600 dark:text-gray-400 rounded-full">
                #{tag}
              </span>
            ))}
          </div>
        )}
      </header>

      <div class="prose prose-lg dark:prose-invert max-w-none">
        {column.content.split('\n').map((line) => {
          if (line.startsWith('## ')) {
            return <h2 class="text-2xl font-bold text-gray-900 dark:text-white mt-8 mb-4">{line.slice(3)}</h2>;
          } else if (line.startsWith('### ')) {
            return <h3 class="text-xl font-semibold text-gray-900 dark:text-white mt-6 mb-3">{line.slice(4)}</h3>;
          } else if (line.startsWith('- ')) {
            return <li class="text-gray-700 dark:text-gray-300 ml-6">{line.slice(2)}</li>;
          } else if (line.trim() === '---') {
            return <hr class="my-8 border-gray-200 dark:border-gray-700" />;
          } else if (line.trim() === '') {
            return null;
          } else {
            return <p class="text-gray-700 dark:text-gray-300 mb-4 leading-relaxed">{line}</p>;
          }
        })}
      </div>
    </article>

    <footer class="mt-12 pt-8 border-t border-gray-200 dark:border-gray-700">
      <a href="/column" class="text-red-500 hover:text-red-600 transition-colors">
        ← 返回专栏
      </a>
    </footer>
  </main>
</Layout>
